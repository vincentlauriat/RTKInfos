import SwiftUI

// Placeholder — sera remplacé en Task 6
// Nécessaire pour que swift build compile
@main
struct RTKMenuBarApp: App {
    var body: some Scene {
        Settings { EmptyView() }
    }
}
